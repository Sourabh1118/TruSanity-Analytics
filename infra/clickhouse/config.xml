<clickhouse>
    <!-- Logging level -->
    <logger>
        <level>warning</level>
    </logger>

    <!-- User-level settings must go in profiles -->
    <profiles>
        <default>
            <max_memory_usage>2000000000</max_memory_usage>
        </default>
    </profiles>
</clickhouse>
